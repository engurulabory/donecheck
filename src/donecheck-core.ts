/**
 * DoneCheck Working Core public boundary.
 *
 * This entry point is intentionally UI-, network-, provider-, persistence-,
 * and credential-independent. Package publication remains disabled until a
 * separate human-approved release decision.
 */
export * from "./domain/donecheck";
